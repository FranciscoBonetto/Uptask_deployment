extends layout

include mixins/crearProyecto
   
block contenido 
    //ponemos un div con una clase
    .contenido-principal
        h1 #{nombrePagina} 

        //pre= vardump(proyecto)

        //Llamamos a error hecho en el controlador
        if errores
            each error in errores
                .alerta= error.texto
            
        //- Se va al mixins
        +crearProyecto(proyecto)
